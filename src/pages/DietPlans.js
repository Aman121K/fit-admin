import React from 'react';
import { Box, Typography } from '@mui/material';

const DietPlans = () => (
  <Box p={3}>
    <Typography variant="h4">Diet Plans</Typography>
    {/* Diet plan management UI will go here */}
  </Box>
);

export default DietPlans; 